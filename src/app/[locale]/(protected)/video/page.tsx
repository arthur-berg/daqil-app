import VideoRoomsList from "@/app/[locale]/(protected)/video/video-rooms-list";
/* import { getAllSessions } from "@/actions/video"; */

const VideoPage = async () => {
  /*   const sessions = await getC();

  if (!sessions) return;

  console.log("sessions", sessions); */

  return <VideoRoomsList />; /*  <VideoBody data={data} />; */
};

export default VideoPage;
